# Pure-Python only where possible: teammates are on MacBook Airs (arm64) and
# nobody should need a compiler. In particular PyMySQL, not mysqlclient.
PyMySQL==1.1.1
cryptography==43.0.3      # required by PyMySQL for MySQL 8 caching_sha2_password
python-dotenv==1.0.1
PyYAML==6.0.2
sqlglot==25.24.0          # parse/validate generated SQL before it touches the DB
httpx==0.28.1             # also satisfies google-genai 2.23.0's >=0.28.1 constraint
google-genai==2.23.0      # Gemini path -- FROZEN 2026-09-18, kept so the
                          # adapter still imports if it is ever thawed. 0.3.0
                          # predates the Client/types API this repo is written
                          # against -- do not pin back.
anthropic>=0.40.0         # Claude backend (haiku/sonnet) -- pipeline smoke tests
rich==13.9.4              # eval report tables
pytest==8.3.3

# Web front end (web/server.py). Pure-Python wheels -- no compiler, so the
# MacBook Airs stay happy. The dashboard itself needs none of these: it also
# renders straight from the file system via web/build_data.py.
fastapi==0.115.6
uvicorn==0.34.0
